AbstractUser — يرث كل شيء من Django's User الافتراضي (username, password, email, first_name, last_name, is_active, date_joined...) ونضيف عليه حقولنا فقط Role.TextChoices — يحدد قيم مسموح بها فقط لـ role. لا يمكن لأحد إدخال دور غير هذه الثلاثة school هو null=True — لأن المدير نفسه قد لا يكون مرتبطاً بمدرسة عند إنشاء حسابه (يُربط لاحقاً) ForeignKey للمدرسة — كل مستخدم ينتمي لمدرسة واحدة، لكن المدرسة فيها عدة مستخدمين (علاقة N:1r